class Absent(str):
    """A target's reason for doing nothing when its app isn't installed or set
    up. Not a problem: setup and theme switches don't mention it, unless the
    target was asked for by name."""
